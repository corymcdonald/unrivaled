    <table>
<% @players.each do |player| %>
    <tr>
        <td>
            <%= image_tag "players/#{player.id}.jpg" %>
        </td>
        <td><%= player.id%> </td>
        <td><%= player.name %></td>
        <td> <%= player.team %></td>
    </tr>
<% end %>
</table>